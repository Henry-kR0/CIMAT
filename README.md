# Proyecto de la CIMAT💻🥷🏻
--------------------------------------------

## ✋🏻🤚🏻Bienvenidos Compañeros, este repositorio de Github es creado con el fin de trabajar el código de la página web que usaremos en nuestro proyecto para la CIMAT.🏫🏁

### Ánimos Compañeros, sé que podemos✊🏻

```
semh-project/
├── backend/
│   ├── server.js         → arranca la API y sirve el dashboard
│   ├── routes/api.js      → endpoints (GET estado/historial/sismo/clima, POST evento)
│   ├── data/mockData.js   → datos de prueba (aquí luego conectarás SQLite/MySQL)
│   └── package.json
└── frontend/
    ├── index.html         → estructura del dashboard (igual a tu mockup)
    ├── css/styles.css     → paleta oscura + tarjetas, igual al diseño que ya ajustaste
    └── js/dashboard.js    → consume la API y pinta el mapa (Leaflet), tarjetas e historial
```
```
